# Local development address

This project uses http://localhost:3107. The shared assignment registry and
browser directory live in `~/Sites/project-template/config/local-projects.yml`
and http://localhost:3099. Explicit port overrides are still supported for
intentional worktrees; reserve a unique assignment there first.

Restart existing development servers to adopt the new default.
